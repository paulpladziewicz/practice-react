import React from "react";
// import styles from '../styles/TodoComplete.module.css'

const TodoComplete = ({checked}) => {

    // state

    // function(s)

    // render
    return (
        <div>
            
        </div>
        )
}

export default TodoComplete